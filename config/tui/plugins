" Get VIM-PLUG plugin manager
if empty(glob("~/.local/share/nvim/site/autoload/plug.vim"))
    execute '!curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
endif

call plug#begin()
" The default plugin directory will be as follows:
"   - Vim (Linux/macOS): '~/.vim/plugged'
"   - Vim (Windows): '~/vimfiles/plugged'
"   - Neovim (Linux/macOS/Windows): stdpath('data') . '/plugged'
" You can specify a custom plugin directory by passing it as the argument
"   - e.g. `call plug#begin('~/.vim/plugged')`
"   - Avoid using standard Vim directory names like 'plugin'

Plug 'junegunn/fzf', { 'do': { -> fzf#install() } }
Plug 'junegunn/fzf.vim'

Plug 'neovim/nvim-lspconfig'

" TODO: get rid of this plugin (implement this functionality manually)
Plug 'RRethy/vim-illuminate'

Plug 'hrsh7th/cmp-nvim-lsp'
"  TODO: try 'hrsh7th/cmp-buffer' for completion in 'search in file'
Plug 'hrsh7th/cmp-path'
Plug 'hrsh7th/cmp-cmdline'
Plug 'hrsh7th/nvim-cmp'

Plug 'hrsh7th/cmp-vsnip'
Plug 'hrsh7th/vim-vsnip'

Plug 'ray-x/lsp_signature.nvim'

" TODO: setup debugger support via DAP
" TODO: setup diff tool (diff file against repository)
" TODO: project compilation and errors in quickfix [c++, go]
" TODO: setup specific key maps via 'AutoKey' application to avoid nvim-qt usage (after this nvim from terminal can be used)

call plug#end()



" Plugin settings
source $HOME/.config/nvim/tui/plugin_settings/fzf
source $HOME/.config/nvim/tui/plugin_settings/nvim-cmp
source $HOME/.config/nvim/tui/plugin_settings/nvim-lspconfig


" <<<<<<<<<<<<<<<<<<<<<<<<<<<<<<
    " [TODO] to see the following colorscheme: https://github.com/ray-x/aurora
    " https://github.com/itchyny/lightline.vim
    " https://github.com/tyrannicaltoucan/vim-quantum

    " TODO: try 'Nord' theme
    " TODO: try statusline
    "    https://github.com/itchyny/lightline.vim
    " TODO: try git_gutter https://github.com/airblade/vim-gitgutter    (possibly it is better than diff tool)

" fuzzy finding of symbols: https://github.com/RishabhRD/nvim-lsputils
" https://github.com/nvim-lualine/lualine.nvim     statusline. also look at themes from this plugin and select one


" TODO: https://github.com/folke/trouble.nvim   -- try to use trouble window with grouping-by-file instead of plain quickfix

" TODO try 'kyazdani42/nvim-web-devicons'

"------------------
"DAP:
"  https://github.com/mfussenegger/nvim-dap
"  https://github.com/rcarriga/nvim-dap-ui
"  https://github.com/danielnehrig/nvim

"or
"  https://github.com/puremourning/vimspector
